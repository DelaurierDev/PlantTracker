#ifndef H_PLANT
#define H_PLANT


#include <iostream>
#include <fstream>
#include <string>

// all definitions in "plantImp.cpp"

class plants {
	int plantChoice = 0; // variable used for user control
	int plantControl = 0; // variable used for exception handling.
public:
	
	void plantInformation();
	// This is the function used for when the user picks option number one from the menu.  It takes whichever option the user wants to learn about and prints out a little paragraph
	// about the type of plant they pick.
	//
};

#endif H_PLANT
